# Maximum clip duration in seconds (hard cap when limit60 is true)
MAX_CLIP_SECONDS=60

# FFmpeg process timeout in seconds
FFMPEG_TIMEOUT_SECONDS=120

# Maximum concurrent FFmpeg processes
MAX_CONCURRENT=1

# Temporary file directory
TMP_DIR=/tmp

# Server port
PORT=3000
